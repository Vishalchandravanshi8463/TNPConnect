<?xml version="1.0" encoding="utf-8"?>
<shape android:shape="rectangle"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke android:width="1dp" android:color="@color/orangeDark"/>
    <solid android:color="@color/orangeDark" />  <!-- Background color -->
    <corners android:radius="20dp" /> <!-- Adjust the radius as per your requirement -->
</shape>
